const wall=document.querySelector('#wall'),viewport=document.querySelector('#viewport'),empty=document.querySelector('#empty'),dialog=document.querySelector('#editor'),canvas=document.querySelector('#pixelCanvas'),ctx=canvas.getContext('2d'),color=document.querySelector('#color'),nameInput=document.querySelector('#name'),placing=document.querySelector('#placing'),toast=document.querySelector('#toast');
let pixels=Array(4096).fill(''),tool='pen',drawing=false,pending=null;ctx.imageSmoothingEnabled=false;
function draw(){ctx.clearRect(0,0,64,64);pixels.forEach((c,i)=>{if(c){ctx.fillStyle=c;ctx.fillRect(i%64,Math.floor(i/64),1,1)}})}
function paint(e){const r=canvas.getBoundingClientRect(),x=Math.max(0,Math.min(63,Math.floor((e.clientX-r.left)*64/r.width))),y=Math.max(0,Math.min(63,Math.floor((e.clientY-r.top)*64/r.height)));pixels[y*64+x]=tool==='eraser'?'':color.value;draw()}
canvas.addEventListener('pointerdown',e=>{drawing=true;canvas.setPointerCapture(e.pointerId);paint(e)});canvas.addEventListener('pointermove',e=>drawing&&paint(e));canvas.addEventListener('pointerup',()=>drawing=false);
document.querySelector('#add').onclick=()=>{pixels=Array(4096).fill('');draw();nameInput.value=localStorage.getItem('guestName')||'';dialog.showModal()};
document.querySelectorAll('[data-tool]').forEach(b=>b.onclick=()=>{tool=b.dataset.tool;document.querySelectorAll('[data-tool]').forEach(x=>x.classList.toggle('active',x===b))});
document.querySelector('#clear').onclick=()=>{pixels.fill('');draw()};
document.querySelector('#place').onclick=()=>{if(!nameInput.reportValidity())return;if(!pixels.some(Boolean))return notify('Zeichne zuerst etwas.');localStorage.setItem('guestName',nameInput.value.trim());pending={name:nameInput.value.trim(),pixels:JSON.stringify(pixels)};dialog.close();placing.classList.add('show');wall.style.cursor='crosshair'};
document.querySelector('#cancelPlace').onclick=()=>stopPlace();function stopPlace(){pending=null;placing.classList.remove('show');wall.style.cursor='';}
wall.addEventListener('click',async e=>{if(!pending)return;const r=wall.getBoundingClientRect(),x=Math.max(0,Math.min(4032,Math.round((e.clientX-r.left)/8)*8)),y=Math.max(0,Math.min(4032,Math.round((e.clientY-r.top)/8)*8));const payload={...pending,x,y};stopPlace();try{const res=await fetch('api/entries',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}),data=await res.json();if(!res.ok)throw new Error(data.error);render(data.entry);notify('Eintrag veröffentlicht. Danke!')}catch(err){notify(err.message||'Speichern fehlgeschlagen.')}});
function render(entry){empty.hidden=true;const c=document.createElement('canvas');c.width=c.height=64;c.className='entry';c.style.left=`${entry.x}px`;c.style.top=`${entry.y}px`;const d=new Date(entry.created_at).toLocaleString('de-DE');c.dataset.label=`${entry.name} · ${d}`;const cx=c.getContext('2d');JSON.parse(entry.pixels).forEach((v,i)=>{if(v){cx.fillStyle=v;cx.fillRect(i%64,Math.floor(i/64),1,1)}});wall.append(c)}
function notify(message){toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3500)}
async function load(){try{const r=await fetch('api/entries'),d=await r.json();d.entries.forEach(render);viewport.scrollTo(1720,1720)}catch{notify('Einträge konnten nicht geladen werden.')}}load();
